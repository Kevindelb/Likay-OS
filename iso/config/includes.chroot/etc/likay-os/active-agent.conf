# Qué agente muestra el kiosco -- ver usr/local/bin/likay-kiosk-start.
# Este es el default de BUILD: kal-in sigue horneado directo en el
# medio live (ver 0320-install-kal-in.chroot, kal-in-backend.service),
# así que apunta a su puerto de siempre. Una instalación real de un
# agente vía agent-install-launcher (ver docs/AGENT_INTERFACE.md)
# sobreescribe este archivo con el puerto del agente que se activó.
AGENT_URL=http://127.0.0.1:8000
